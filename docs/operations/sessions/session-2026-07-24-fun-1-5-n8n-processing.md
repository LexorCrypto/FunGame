# Session 2026-07-24 — FUN-1…FUN-5 (M1 + старт M2), SPEC-фикс, инфра Linear-событий

## Что сделано

**Игра (Linear, команда FUN, проект «Pissuarius v1»):**
- **FUN-1** Каркас: game.html (CDN phaser@3.90.0, пин), конфиг 480×270 pixelArt FIT+CENTER_BOTH, целочисленный зум snap на resize; звёздный фон 3 слоя (30/20/12, 10/25/45 px/s, alpha 0.4/0.7/1.0). Коммит 1c4c2b5.
- **FUN-2** Спрайты: src/data/sprites.js — палитра (15 символов) + 23 спрайта / 37 кадров дословно из SPEC §11; scripts/validate_sprites.mjs. Коммит daabdff.
- **SPEC §11 fix:** номера пяти босс-дельт приведены к строкам их меток (глаза 7–8, ноги 11–12, блик/самоцвет 17, рот 10) — решение владельца SPEC; sprites.js синхронизирован. Коммит 5f9faa9.
- **FUN-3** BootScene (37 canvas-текстур из схем) + корабль: position-based WASD 140 px/s, границы x[8,472] y[184,262], огонь 300 px/s, кулдаун 250 ms, макс 4 снаряда. Коммит a1e181a.
- **FUN-4** Формация: сетка 10×4, ячейка 24×18, origin (120,40), sway 20·sin(2πt/period) единый, без спуска/ускорения; один таракан. Коммит db7b47e.
- **FUN-5** Коллизии и жизни: Enemy (взрыв 8–12 частиц), Effects (частицы + гашение ×0.9/кадр через custom particleClass; shake ровно 6 px), 3 жизни, респаун 1.5 s, неуязвимость 2.0 s мерцание 8 Гц, 3-я смерть → fade-out + pause (заглушка до FUN-20). Коммиты 154b123, 9fc4daf.

**Инфра (обработка событий Linear):**
- Обработка входящих событий Linear настроена и проверена. Операционные детали — во внутренних заметках, НЕ в публичном репо.

**Решения владельца:** правка SPEC §11 «по меткам» (игровое). Решения по инфраструктуре обработки событий — во внутренних заметках.

## Проверки

- Спрайты: независимый парсер SPEC → 1:1 по всем кадрам; self-validate exit 0.
- Игра (браузер, каждая задача): FUN-1 — зум ровно 3/2, звёзды; FUN-3 — границы точно 8/472/184/262, шаг снарядов 75 px; FUN-4 — sway max 19.98 px синус, y const; FUN-5 — все overlap'ы, тайминги респауна/неуязвимости, мерцание, fade+pause после 3-й смерти; частицы с гашением ×0.9 замерены. 0 консольных ошибок везде.
- Инфра: обработка событий Linear проверена боевыми событиями. Детали — во внутренних заметках.

## Уроки

- Phaser 3.90: velocityX/Y emitter-опов нет (configOpMap игнорирует) — damping на custom particleClass; camera.shake intensity = доля вьюпорта.
- Инфра-уроки (обработка событий Linear, хранилище, уведомления) — во внутренних заметках.

## Close-session codex-аудит (2026-07-24)

Прогон по 7 фича-коммитам (3b95e17..9fc4daf) → ITERATE, два P1 + фиксы (аудит-фиксы 26e174e/48783c9/3297086 аудированы отдельно):
- **P1** integer-zoom CSS-override канваса после FIT ломал центрирование/pointer на немultiple-вьюпортах → parent-container integer zoom + deferred refresh (26e174e).
- **P1** 3-я смерть молча перезапускала сцену → fade-out + pause до FUN-20 (26e174e).
- **P2** Enemy hp не декрементился (HP>1 умирал бы с одного выстрела) → takeDamage (26e174e); взрыв одним тинтом → массивы цветов спрайта (26e174e).
- **P2** fallback `100vh` (large viewport, browser chrome) → измеренные px visualViewport (48783c9).
- **P2/P3** центрирование по `min-height:100vh` (large viewport) + pinch-zoom дёргал k → высота body в измеренных px, гард `visualViewport.scale !== 1` (3297086).
- Отложено: P3 bossPlumber B (нет literal-схемы — решение владельца), P2 title в game.html (константа по §15).
Ре-аудиты: 26e174e → ITERATE (только P2 vh, закрыт в 48783c9); 48783c9 → ITERATE (P2 body min-height + P3 pinch, закрыты в 3297086); 3297086 → см. артефакт. Артефакты: docs/operations/audits/*-codex-audit.md.

## Дальше

- FUN-6 «Пикирование: DivePatterns + дайв-дирижёр» (M2).
- FUN-22 потребует свежий ELEVENLABS_API_KEY от владельца к моменту задачи.
- Push всех локальных коммитов (FUN-1…FUN-5 + SPEC-фикс + аудит-фиксы + close-артефакты) — по команде владельца.
