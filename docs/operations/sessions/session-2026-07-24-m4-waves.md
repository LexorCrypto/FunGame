# Session — 2026-07-24 — M4 «Волны» (FUN-9 / FUN-10 / FUN-11)

## Итог

Реализован, проверен и закрыт майлстоун **M4 «Волны»**: данные волн, волновой
дирижёр с баннерами и Босс 1 «Супер-Туалет». Три атомарных тикета Linear закрыты
(Done + приёмочные комментарии). Один фикс-фиделити добавлен по аудиту. Четыре
локальных коммита, не запушены (пуш — по команде владельца).

## Сделано

- **FUN-9** `src/data/waves.js` — 25 записей волн дословно по таблице SPEC §6
  (rows/swayPeriod/diveInterval/maxDivers/bulletSpeedMul; босс-волны `{ boss }`
  на 5/10/15/20/25) + бесконечный цикл (`CYCLE_WAVES`, `GOLDEN_THRONE_WAVE`,
  `applyCycleModifiers`, `scoreMultiplier`, `actForWaveNumber`). Коммит `ba8f452`.
- **FUN-11** `src/entities/Boss.js` — Босс 1 «Супер-Туалет»: 100 HP, y=60, sway
  ±60; фаза 1 (веер 5 капель 90 px/s ±30° / 2.0 s, смыв-воронка / 8.0 s на 3.0 s,
  тяга игрока 30 px/s к центру) → фаза 2 при HP≤50% (веер 7 / 1.5 s, смыв / 6.0 s
  на 4.0 s, период sway 2.0 s), вспышка+тряска+пауза 1.0 s, HP-бар 120×4 Y→R.
  Плюс `Player.pullToward` для воронки. Коммит `75d88e0`.
- **FUN-10** `src/systems/WaveDirector.js` — подача волн в строй (ряд `rows[i]` →
  ряд сетки, центровка; count>10 расширяет ряд), баннеры ВОЛНА/АКТ/ВНИМАНИЕ +
  вход в цикл ПОБЕДА!/ЦИКЛ, переходы по зачистке/гибели босса, недоступный босс —
  явное состояние «недоступно» (без throw, не «зачищено»). Интеграция в `main.js`
  (пустой строй + группа боссов + оверлапы + фабрика), `bulletSpeedMul` в
  `DivePatterns.js`. Коммит `ed4258d`.

## Проверка

- **Unit (node + заглушка Phaser, /tmp):** `waves.js` — каждая волна 1:1 с
  независимо транскрибированной таблицей §6, суммы ≤40, модификаторы цикла,
  акты — все зелёные. `Boss`+`WaveDirector` — обе фазы, веер 5→7 @90 px/s,
  переход при HP≤50% (fanCount/sway/interval/HP-бар/тряска), пауза подавляет
  огонь, воронка тянет игрока, смерть эмитит `boss-defeated`; центровка рядов,
  fallback count>10, недоступный босс (без исключения, не зачищается), баннеры
  цикла — все зелёные.
- **Браузер (реальный модульный граф `src/main.js`):** `game.html` тянет Phaser с
  CDN, а он НЕ грузится в песочном headless-браузере, поэтому проверка велась через
  временный локальный хост (`eval` вендоренного Phaser 3.90 + динамический
  `import('src/main.js')` — тот же НЕИЗМЕНЁННЫЙ код/BootScene, что и в `game.html`).
  0 консольных ошибок. Волна 1: row0=8 таракан, row1=8 какаха (центрировано), баннер
  «ВОЛНА 1», параметры совпадают. Босс: hp100, y60, HP-бар жёлтый, в группе коллизий,
  веер из 5 (enemyProjActive=5). HP≤50% → фаза 2 (fan 7, sway 2.0, бар красный,
  ширина 60). Смерть → волна 6 (24 врага: вантуз×8+таракан×8+писсуар×8) + «АКТ 2».
  **Ограничение окружения:** сам `game.html` (с CDN-Phaser) в этой песочнице не
  проверялся — проверен эквивалентный локальный бут того же кода.

## Аудит (codex gpt-5.6-sol, read-only)

Артефакты: `docs/operations/audits/{ba8f452,75d88e0,ed4258d,0beea55}-codex-audit.md`.

- **ed4258d [P1] «soft-lock на волне 10» → ПРИНЯТО как намеренная граница M4**
  (не дефект). Боссы 2–6 — будущие тикеты FUN-14…19; нереализованный босс входит
  в явное состояние «недоступно» по требованию ревью (никогда не throw, никогда не
  «зачищено»). Волна 10 — текущий фронт контента игры в разработке; полный проход
  разблокируется по мере регистрации боссов в фабрике.
- **Прочие [P2]/[P3] отложены** (см. known-issues в `status.md`): комбат во время
  баннеров (дизайн, §14 замораживает только Паузу), накопление неактивных объектов
  (жизненный цикл врага, FUN-6/7/8 — отдельный пуллинг), хардкод RU-строк (i18n
  §15/FUN-18), музыка босса (аудио §12/FUN-22), веер-7 vs cap-12 (§3, деградация),
  слушатель SHUTDOWN после `die()`, модификаторы цикла для Золотого Трона (нереализован).
- **Исправлено по аудиту (коммит `0beea55`, вердикт PASS):** воронка босса
  замораживается на 1-с паузу смены фазы; sway непрерывен при смене периода (нет
  скачка позиции). Проверено unit + браузер (живой скачок 0.76 px, воронка заморожена).

## Git / Linear

- Ветка `main`, **7 локальных коммитов впереди `origin/main`** (FUN-6…11 + фикс `0beea55`), НЕ
  запушено. Рабочее дерево: изменён только `.planning/.continue-here.md`
  (унаследованный хэндофф).
- Linear (команда FUN, «Pissuarius v1»): FUN-9 / FUN-11 / FUN-10 → **Done** с
  приёмочными комментариями.

## Дальше

- M5+ по майлстоунам (FUN-12…): следующие боссы (FUN-14…19) снимут границу волны 10.
- Отложенный полишинг из аудита — при доработке боссов/волн (см. known-issues).
- Пуш 7 коммитов — по команде владельца.
