# Codex audit — a2b94f9

- **Commit:** `a2b94f9` — docs: SPEC §17 без переоценки — реальный период топота, таблица кадансов Трона, y круга Макаки (codex-аудит f691893)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** CHANGES REQUESTED — 1×P2, 3×P3.

## Findings (verbatim)

Кратко:

- **[P2] [docs/SPEC.md:933](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/docs/SPEC.md:933)** — утверждение, что фаза 2 «каданс не трогает», неверно. `onEnterPhase2()` вызывает `resetCadence()` ([BossGoldenThrone.js:584](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/src/entities/BossGoldenThrone.js:584)): накопленные интервалы веера/дуг/ключа теряются, призыв становится немедленно готовым, а `plopFiredThisWindow` сбрасывается. Если фаза сменится после первого плюха в окне, возможен второй плюх, что прямо противоречит [docs/SPEC.md:941](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/docs/SPEC.md:941). Нужно либо описать reset и исключение, либо исправить код.

- **[P3] [docs/SPEC.md:920](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/docs/SPEC.md:920)** — период `≈5.7 s` верен только в steady state. При входе в фазу 2 `stompTimer` обнуляется, а фазовая пауза замораживает уже начатый прыжок, поэтому start-to-start может быть около `6.7 s` и более. Для нормативного SPEC следует явно оговорить фазовый edge-case.

- **[P3] [docs/SPEC.md:928](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/docs/SPEC.md:928)** — нормативный SPEC фиксирует `y=230`, одновременно называя значение временным. GitHub issue #2 остаётся открытым, оба варианта решения не выбраны. Это противоречит заявлению §17, что открытые вопросы не живут в SPEC. Нужен зафиксированный выбор владельца либо явное определение `y=230` как действующего контракта до пересмотра.

- **[P3] [CLAUDE.md:51](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:51)** — источник задач описан неоднозначно: здесь все задачи помещены в GitHub Issues, тогда как строки 16–23 разделяют задачи Linear и только открытые вопросы/аудиты/приёмку GitHub. Это может направить будущий `close_session` не в тот трекер.

**Вердикт: CHANGES REQUESTED.** [P1] нет, но [P2] делает новую таблицу кадансов фактически неверной для перехода фазы.

Измененные файлы:

- Нет — аудит read-only.

Проверки:

- `git show a2b94f9` и сопоставление с реализацией боссов.
- `git diff --check` — PASS.
- Скан добавленных строк на ключи, токены, пароли и private keys — чисто.
- Новых ресурсов, таймеров или слушателей коммит не добавляет: diff документационный.
- Runtime-тесты не запускались — код не менялся.

Git/GitHub:

- Ветка `main`, HEAD `a2b94f9`, опережает `origin/main` на 4 коммита.
- Есть два несвязанных untracked audit-файла; я их не трогал.
- Issue #2 проверен read-only, он открыт.

Риски и следующие шаги:

- LightRAG был недоступен по сети.
- Serena-поиск работал, но symbol overview недоступен из-за отсутствия language server; использовано точечное построчное чтение.

## Disposition

- **[P2] Утверждение «фаза 2 каданс не трогает» неверно: `onEnterPhase2()` → `resetCadence()` сбрасывает счётчики и флаг `plopFiredThisWindow`, из-за чего на границе фаз возможен второй плюх в том же окне → ЧАСТИЧНО ИСПРАВЛЕНО** в `336b011`. Документация приведена к фактическому поведению; сам код НЕ тронут намеренно: это правка боевой механики на границе фаз, а не техническая, и решение за владельцем — заведена [issue #10](https://github.com/LexorCrypto/FunGame/issues/10) с двумя вариантами.
- **[P3] Период топота ≈5.7 s верен только в устойчивом режиме → ИСПРАВЛЕНО** в `336b011`: оговорена граница фаз (обнуление таймера + заморозка начатого прыжка, ≈6.7 s и более).
- **[P3] SPEC фиксировал `y=230`, одновременно называя его временным → ИСПРАВЛЕНО** в `336b011`: это действующий контракт до пересмотра, нерешённый вопрос живёт в issue #2, а не в SPEC.
- **[P3] Неоднозначность трекера в `CLAUDE.md` → ИСПРАВЛЕНО** в `336b011`: живой трекер — GitHub Issues (задачи, вопросы, аудиты, приёмка), Linear — архив закрытых M1–M9.
