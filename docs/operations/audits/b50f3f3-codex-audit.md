# Codex audit — b50f3f3

- **Commit:** `b50f3f3` — docs: хэндофф закрывает GitHub Issue, а не Linear; валидатор STATE — в скилле (codex-аудит 5379573)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** REQUEST CHANGES — 1×P1, 1×P2, 1×P3.

## Findings (verbatim)

## Кратко

- **[P1] [CLAUDE.md:85](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:85)** — ручной fallback сводит security-гейт к сверке имён полей. Реальный валидатор также проверяет типы значений, обязательные поля, заголовок, singleton-маркер и отсутствие постороннего текста. Поэтому секрет в разрешённом поле может пройти описанную ручную проверку и попасть в публичный STATE issue. **Вердикт: блокирует принятие.** Убрать fallback либо документировать весь fail-closed контракт.

- **[P2] [.planning/.continue-here.md:13](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/.planning/.continue-here.md:13)** — цикл безусловно предписывает закрыть GitHub Issue, хотя [CLAUDE.md:59](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:59) разрешает любые мутации issues только по явной команде. Кроме того, issue закрывается после локального коммита, но до разрешённого push/deploy; для игровых задач это позволяет объявить приёмку до выполнения требования SPEC §16 о работе с GitHub Pages. **Вердикт: исправить.** Закрывать issue только по явной команде и после публикации/проверки.

- **[P3] [CLAUDE.md:83](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:83)** — удалена единственная рабочая форма запуска валидатора с `--profile typed-pointer-v1`. Без этого обязательного аргумента валидатор всегда возвращает `mfa_profile is required`, поэтому инструкция «валидатор живёт здесь» недостаточна для ручного обслуживания. **Вердикт: исправить документацию**, добавив точную команду.

Итоговый вердикт: **REQUEST CHANGES**. Перенос живого трекера в GitHub соответствует [SPEC §17](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/docs/SPEC.md:913), но P1/P2 делают процесс небезопасным и внутренне противоречивым.

## Изменённые файлы

Аудит read-only, файлов не менял. Коммит затрагивает:

- `.planning/.continue-here.md`
- `CLAUDE.md`

## Проверки

- `git show b50f3f3`, `git show --check` — whitespace-ошибок нет.
- Скан добавленных строк на распространённые форматы секретов — совпадений нет.
- Негативная проба валидатора подтвердила, что проверка только имён полей пропускает то, что trusted-валидатор отвергает.
- Утечки ресурсов/слушателей отсутствуют: runtime-код не изменялся.
- Build/tests не запускались: коммит только документальный.

## Git/GitHub

- Ветка: `main`, `HEAD=b50f3f3`.
- Есть 9 существующих untracked-файлов; аудит их не затрагивал.
- Внешние GitHub-мутации не выполнялись.

## Риски и следующие шаги

Исправить три пункта выше и повторно проверить согласованность handoff-процесса с правилами явного разрешения и SPEC §16. LightRAG проверить не удалось: сеть sandbox не подключается к серверу.

## Disposition

- **[P1] Ручной fallback сводил гейт к сверке имён полей — секрет в разрешённом поле мог попасть в публичный STATE → ИСПРАВЛЕНО** в `16e779c`: fallback убран (нет скилла — тело #12 не трогаем), добавлено правило «в теле только ссылки, SHA и id» и обязательное перечитывание с повторной проверкой после записи.
- **[P2] Хэндофф безусловно предписывал закрывать issue после локального коммита — внешняя мутация без команды владельца и приёмка до прода вопреки §16 → ИСПРАВЛЕНО** в `16e779c`: закрытие только после пуша и прод-прогона §16, и только по команде владельца.
- **[P3] Пропала форма запуска валидатора с обязательным `--profile` → ИСПРАВЛЕНО** в `16e779c`: приведена точная команда.
