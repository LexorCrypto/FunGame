# Codex audit — 808d6e4

- **Commit:** `808d6e4` — fix(process): гейт STATE проверяет КАНДИДАТА до записи (codex-аудит 16e779c)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** FAIL / REQUEST CHANGES — 1×P1, 1×P2.

## Findings (verbatim)

Находки:

- [P1] `CLAUDE.md:89` — путь передан позиционно, но `validate_state_mfa.py` читает файл только через `--file`. Команда проверяет `stdin`, а не кандидата: обычно зависает/падает, но при валидном постороннем stdin может разрешить публикацию непроверенного тела, включая секрет. Воспроизведено. Исправление: `--file /tmp/state-body.md`. Уже устранено следующим коммитом `afe5422`.

- [P2] `CLAUDE.md:88` — предсказуемый `/tmp/state-body.md` остаётся после проверки и не требует закрытых прав. Если валидатор отклонит случайно попавший секрет, он сохранится в локальном временном файле. Нужны `mktemp`, права `0600` и гарантированный cleanup.

Вердикт: **FAIL / REQUEST CHANGES** для `808d6e4`.

Кратко:

- `docs/SPEC.md` не регулирует close-session/STATE; противоречий игровой спецификации нет.
- Утечек слушателей нет; обнаружен только риск утечки временного файла.
- В самом diff секретов и типовых токенов не обнаружено.

Изменённые файлы:

- `CLAUDE.md`

Проверки:

- `git show 808d6e4`
- Прямое воспроизведение ошибочного CLI-вызова
- `git show --check` — PASS
- Проверка diff на распространённые форматы секретов — совпадений нет

Git/GitHub:

- Ветка `main`, на 12 коммитов впереди `origin/main`
- Были существующие untracked smoke/audit-файлы; изменений не вносил
- GitHub не изменялся.

## Disposition

- **[P1] Путь к кандидату передавался позиционно, а валидатор читает файл только через `--file` → ИСПРАВЛЕНО** в `afe5422`: команда проверяла бы stdin, а не кандидата. Обе формы (`--file`, `--issue`) прогнаны на реальных данных.
- **[P2] Предсказуемый `/tmp/state-body.md` переживал проверку → ИСПРАВЛЕНО** в `34fdd2f` и доведено в `7ed0267`: `umask 077` + `mktemp` с явным шаблоном + `trap` на удаление обоих файлов; проверено, что оба создаются как `-rw-------` и после выхода не остаются.
