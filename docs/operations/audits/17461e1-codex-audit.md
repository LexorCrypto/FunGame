# Codex audit — 17461e1

- **Commit:** `17461e1` — fix: пауза не встревает в переход сцены, лупы воспроизводимы скриптом (codex-аудиты 98b4734/fb7a242)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** REQUEST_CHANGES** — один P2 в основном обещании коммита: скрипт воспроизводимости способен дать false pass для несшитого лупа.

## Findings (verbatim)

## Находки

- **[P2] [scripts/trim_music_loops.mjs:107](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/scripts/trim_music_loops.mjs:107)** — `--check` проверяет только `gapMs > 16`, игнорируя скачок амплитуды на стыке. Поэтому регенерированный трек без тишины, но с резким несовпадением первого и последнего семплов, будет пропущен без 4 ms fade и завершится с кодом 0. Это противоречит SPEC §12: стык должен проходить через нуль без щелчка, заявленный скачок — ≤0.00015. Дополнительно PCM декодируется через FFmpeg на [строке 39](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/scripts/trim_music_loops.mjs:39), хотя нормативная метрика SPEC требует браузерный `decodeAudioData`. Нужна проверка `jump` и отдельная браузерная приёмка либо всегда применяемый post-processing после регенерации.

**[P1]:** не обнаружено.  
**[P3]:** не обнаружено.

## Вердикт

**REQUEST_CHANGES** — один P2 в основном обещании коммита: скрипт воспроизводимости способен дать false pass для несшитого лупа.

Исправление паузы корректно: `leaving` закрывает окно между постановкой и исполнением перехода; cleanup снимает `window.keydown`, `SHUTDOWN` и `DESTROY`. Новых утечек слушателей или ресурсов не найдено. Секретов нет.

## Проверки

- `git show 17461e1`, сверка с `docs/SPEC.md` §12/§14/§16.
- `git diff --check` — чисто.
- `node --check scripts/trim_music_loops.mjs` — успешно.
- `node scripts/trim_music_loops.mjs --check` — exit 0, текущие gap 8.1/12.2/11.3 ms.
- Gitleaks 8.30.1: 1 commit, утечек нет.
- Serena активирована, но JS language server отсутствует; использован точечный Git/`rg`.

## Git/GitHub

- Ветка `main`, `HEAD=17461e1`, ahead `origin/main` на 12.
- Есть существующие untracked audit-файлы; аудит ничего не изменял.
- Issue/PR не указаны, GitHub не изменялся.

## Disposition

- **[P2] `--check` игнорировал скачок амплитуды на стыке (false pass для несшитого лупа) → ИСПРАВЛЕНО** в `dca51ea`: добавлен второй критерий (скачок ≤ 0.001 из 1.0) в оба режима, длина фейда наращивается 4 → 12 → 30 → 80 ms, при неудаче — ненулевой код и требование перегенерировать трек. Негативный тест: искусственно обрезанный трек (разрыв 0.0 ms, скачок 0.13022) теперь валит `--check`, а прогон доводит скачок до 0.00064.
