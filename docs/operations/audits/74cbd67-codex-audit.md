# Codex audit — 74cbd67

- **Commit:** `74cbd67` — docs: M7 audio FUN-21/23 done — status snapshot + FUN-22 handoff
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** ITERATE** — 1 блокирующая P1, 2 P2, 1 P3.

## Findings (verbatim)

## Находки

- [P1] `scripts/generate_audio.mjs:70` — длительности SFX передаются в API без нормализации. Шесть обязательных эффектов имеют 0.2–0.4 s, тогда как актуальный endpoint принимает минимум 0.5 s. Они будут отклонены, скрипт завершится с ошибкой и FUN-22 не создаст все 22 файла. Это противоречит `docs/SPEC.md:697-698`, требующим использовать ближайший эквивалент актуального API, и делает утверждение `status.md:30` о готовности генератора неверным. Нужен clamp до 0.5 s либо генерация 0.5 s с постобрезкой. [Документация ElevenLabs](https://elevenlabs.io/docs/api-reference/text-to-sound-effects/convert).

- [P2] `status.md:65` — документ одновременно объявляет FUN-23 завершённым (`status.md:29-33`) и утверждает, что аудио/мьют не реализованы. Новая запись о Done превращает оставшийся пункт Known issues в прямое противоречие; следующая сессия не сможет считать `status.md` достоверным.

- [P2] `.planning/.continue-here.md:45` — smoke-рецепт запускает `python3 -m http.server 8177` из корня репозитория без `--bind`. Сервер по умолчанию слушает все интерфейсы и отдаёт весь корень, включая `.git` и возможный gitignored `.env` с ElevenLabs-ключом. Использовать как минимум `--bind 127.0.0.1` и ограниченную директорию. [Документация Python](https://docs.python.org/3.12/library/http.server.html). Строка существовала до коммита, но осталась в обновлённом handoff.

- [P3] `src/systems/Audio.js:221` — модульный singleton `audioSystem` не сбрасывается при `game.events.destroy`. Глобальный keydown снимается, но singleton продолжает удерживать уничтоженный `Phaser.Game`; создание второй игры на той же странице вернёт старую аудиосистему, сломает звук/M и удержит старый граф объектов. Для обычной единственной игры не проявляется.

Коммит `74cbd67` сам docs-only; P1 и P3 обнаружены при проверке реализаций `be668f7`/`aff0c17`, которые новые документы объявляют завершёнными.

## Вердикт

**ITERATE** — 1 блокирующая P1, 2 P2, 1 P3.

Остальная часть соответствует SPEC: manifest 16+6, промпты и имена совпадают, музыка loop только для title/battle/boss, mute хранится в `localStorage`, штатные слушатели Title/M снимаются. Реальных секретов в коммите или дереве не найдено — только placeholder.

Кратко:

- Репозиторий не изменялся.
- Проверены `git show`, точное дерево коммита, `git diff --check`, синтаксис Audio.js/generate_audio.mjs и шаблоны секретов.
- Serena была доступна, но без language server для JavaScript; Context7 недоступен, поэтому API сверялось с официальной документацией.

Git/GitHub:

- Ветка: `main`.
- Рабочее дерево содержит более поздние пользовательские изменения и smoke-файлы; они не учитывались как часть `74cbd67`.
- Push/комментарии/PR не выполнялись.

## Disposition

- **[P1] Кламп длительности SFX → ЗАКРЫТО** коммитом `c2ba43e` (та же находка, что в `be668f7`).
- **[P2] `status.md` одновременно объявляет FUN-23 сделанным и «аудио не реализовано» → ИСПРАВЛЕНО** в этой сессии: раздел Known issues пересобран, устаревшие пункты сняты.
- **[P2] Рецепт smoke поднимал `python3 -m http.server` без `--bind` (весь корень репо на всех интерфейсах) → ИСПРАВЛЕНО**: в `.planning/.continue-here.md` рецепт теперь с `--bind 127.0.0.1`.
- **[P3] Синглтон аудио → ИСПРАВЛЕНО** в `968bb3d`.
