# Codex audit — ba9f497

- **Commit:** `ba9f497` — fix(process): .planning/** не может быть light-tier, точнее про поверхности секретов
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-26
- **Verdict (codex):** FAIL — 1×P3.

## Findings (verbatim)

### Находки

#### [P3] Шаблоны CLAUDE/AGENTS приведены неточно — CLAUDE.md:165

В `INSTRUCTION_SURFACES` находятся `**/CLAUDE.md` и `**/AGENTS.md`, а не указанные в тексте `CLAUDE.md` и `AGENTS.md`. Это занижает описанную область защиты: классификатор охватывает такие файлы на любой глубине.

Предлагаемое исправление: заменить их на точные шаблоны `**/CLAUDE.md` и `**/AGENTS.md`.

### Вердикт

**FAIL — P1: 0, P2: 0, P3: 1.**

Обе исходные находки закрыты по существу:

- `.planning/**` удалён из light-tier;
- `**/.planning/**` действительно находится в неизменяемом `INSTRUCTION_SURFACES`;
- `classify()` возвращает `full` до проверки пользовательских tier-glob’ов;
- оба tier-ключа успешно парсятся: нулевая колонка, одна физическая строка, fenced-блок сохранён;
- формулировка про `ELEVENLABS_API_KEY` теперь соответствует `.gitignore:2`, `README.md:25–27` и SPEC §12;
- `generate_audio.mjs` — единственный исполняемый код, читающий этот секрет;
- новых противоречий с остальным `CLAUDE.md` не обнаружено;
- Gitleaks: секретов нет; новых PII в diff нет;
- `git diff --check` прошёл;
- ветка `main`, рабочее дерево чистое, `main` опережает `origin/main` на 6 коммитов.

LightRAG был недоступен из-за сетевого ограничения; проверка выполнена против текущего локального репозитория и установленного `audit_coverage.py`.

## Disposition

Находка закрыта в `b54a9f5`: соседи по `INSTRUCTION_SURFACES` приведены к фактическим
шаблонам `**/CLAUDE.md` и `**/AGENTS.md` с явной оговоркой, что шаблон ловит файл на любой
глубине. Прежняя формулировка занижала описанную область защиты.

Обе исходные находки аудита `a2a1221` аудитор подтвердил закрытыми по существу, включая то,
что оба tier-ключа по-прежнему парсятся: нулевая колонка, одна физическая строка,
fenced-блок сохранён.
