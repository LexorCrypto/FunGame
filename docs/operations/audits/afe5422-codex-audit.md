# Codex audit — afe5422

- **Commit:** `afe5422` — fix(docs): правильный вызов валидатора STATE — --file/--issue, не голый путь
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** REQUEST CHANGES — 1×P2.

## Findings (verbatim)

[P2] [CLAUDE.md:94](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:94) — `verify-after-write` фактически не реализован. Команда начинается с литерала `…`, поэтому не исполняется напрямую. Даже после раскрытия полного пути `--issue 12` лишь повторно проверяет MF-A-схему: валидатор не сравнивает опубликованное тело или `last_writer_run` с кандидатом. Старое либо конкурентно перезаписанное, но валидное тело даст exit 0. Это нарушает заявленный MF-B-контракт обнаружения clobber/lost update. Нужно перечитать тело, проверить совпадение с записанным кандидатом/идентификатором текущего запуска и обработать несовпадение.

Вердикт: **REQUEST CHANGES** — один P2.

Кратко:

- `--file /tmp/state-body.md` соответствует реальному CLI валидатора;
- `--issue 12` поддерживается, но недостаточен для настоящего verify-after-write;
- `docs/SPEC.md` коммит не меняет и игровым требованиям не противоречит: изменение относится к операционному процессу;
- секретов в diff не найдено; утечки ресурсов и слушателей неприменимы к документационному изменению.

Измененные файлы:

- Коммит: `CLAUDE.md`;
- мной файлы не изменялись.

Проверки:

- изучен полный `git show afe5422`;
- проверены парсер и `--help` `validate_state_mfa.py`;
- выполнены whitespace- и secret-pattern scans;
- тесты не запускались: коммит документационный.

Git/GitHub:

- ветка `main`, на `afe5422`, впереди `origin/main` на 12 коммитов;
- присутствуют ранее существовавшие untracked-файлы;
- GitHub-состояние не изменялось.

## Disposition

- **[P2] verify-after-write был неполноценным: `--issue 12` подтверждает только схему и одинаково доволен чужой валидной перезаписью → ИСПРАВЛЕНО** в `34fdd2f`: настоящая проверка — `diff` кандидата с перечитанным телом, прогон валидатора остался вторым слоем. В `4a80222` расхождение сделано fail-closed (`|| { echo … >&2; exit 1; }`) — раньше клоббер печатался и процедура шла дальше. Негативная проба: подмена в перечитанном теле ловится.
