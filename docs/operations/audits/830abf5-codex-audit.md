# Codex audit — 830abf5

- **Commit:** `830abf5` — docs: M9 local acceptance done — balance sweep clean, full local SPEC-16 run green; FUN-26 awaits push + prod run
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** ITERATE — 4×P2, 1×P3, P1 нет.

## Findings (verbatim)

### Находки

- [P2] `src/main.js:42` — пауза отсутствует. В игровой сцене зарегистрированы только `W,A,S,D,SPACE`; `Esc` работает лишь для пропуска Crawl. Это нарушает `docs/SPEC.md:774,789` (`Game ⇄ Pause`, заморозка мира), поэтому утверждение о локальной приёмке преждевременно. Вердикт: исправить либо явно изменить SPEC.

- [P2] `src/entities/BossSuperPoop.js:70` — таймер «Плюха» растёт только в `idle`. Последовательность telegraph/fall/return занимает ещё 3.3 s, поэтому фактический период составляет примерно 9.3/7.3 s вместо требуемых 6.0/4.0 s (`SPEC:170,174`). Это прямо противоречит добавленному claim «§7 без расхождений» в `status.md:40`. Вердикт: считать интервал от старта атаки либо согласовать новую семантику в SPEC.

- [P2] `src/systems/WaveDirector.js:69` — Золотой Трон получает исходный `GOLDEN_THRONE_WAVE`, без модификаторов цикла. Его атаки не получают `bulletSpeed × 1.1^c`, хотя §6 задаёт этот множитель для бесконечного цикла (`SPEC:139-142`). То же отклонение уже признано в `status.md:59`. Вердикт: claim «расхождений нет» неверен.

- [P2] `src/entities/Enemy.js:72` и `src/entities/BossBase.js:56` — бесконечный режим имеет неограниченное накопление ресурсов. Враги только деактивируются, но каждый следующий спавн создаёт новый объект; за один цикл остаётся около 158 неактивных врагов. Боссы также не уничтожаются, а анонимный `SHUTDOWN`-listener продолжает удерживать каждый экземпляр после `die()`. Поскольку игровая сцена в endless-режиме не завершается, растут группы, память и число слушателей. Вердикт: P3 из `status.md:61` занижен; для заявленного бесконечного режима это P2.

- [P3] `status.md:41` — запись «полный локальный прогон §16» переоценивает покрытие: фактически указаны только волна 1, отдельный паттерн врага и одна фаза босса, тогда как критерии 5/6/10 требуют все волны, типы врагов и боссов. Предложенный финальный prod-smoke также проверяет лишь Boot→Title→игру и Трон. Вердикт: зафиксировать воспроизводимую матрицу проверки либо смягчить claim.

### Итоговый вердикт

**ITERATE — 4×P2, 1×P3, P1 нет.** Коммит документационный, но неверно фиксирует состояние как локально принятое при известных нарушениях SPEC и незакрытом lifecycle leak.

Кратко:

- Diff прочитан полностью; изменены только `.planning/.continue-here.md` и `status.md`.
- Секретов в коммите не найдено: `gitleaks` проверил один commit; `sk_your_key_here` — шаблон из `.env.example`.
- `git diff --check` проходит.

Изменённые файлы:

- Я файлов не изменял.

Проверки:

- Serena symbol-LSP недоступен (`active language servers: []`), поэтому использованы Serena pattern search, `git show 830abf5:<path>` и `rg`.
- Runtime-тесты не запускались: коммит docs-only, аудит выполнен read-only.

Git/GitHub:

- Ветка `main`, локально ahead `origin/main` на 2 коммита.
- В начале дерево было чистым; во время аудита появились сторонние незакоммиченные изменения и smoke-файлы. Они не тронуты; анализ основан исключительно на snapshot `830abf5`.
- GitHub/Linear не изменялись.

## Disposition

- **[P2] Пауза §14 отсутствует → ИСПРАВЛЕНО** в `a669d93`.
- **[P2] Интервал «Плюх» Супер-Какахи считается только в простое (≈9.3/7.3 s вместо 6.0/4.0) → ОТЛОЖЕНО, решение владельца.** Это трактовка «интервал простоя» vs «период старт-к-старту»; смена семантики меняет сложность боя. Пункт уже стоял в Known issues как ожидающий владельца и там остаётся.
- **[P2] Модификаторы цикла не применяются к Золотому Трону → ИСПРАВЛЕНО** в `905bb40` (sway `2.5×0.9^c` мин 1.4, скорость снарядов `×1.1^c`; `diveInterval` аналога не имеет — зафиксировано в SPEC §7.6).
- **[P2] Накопление врагов/боссов → ИСПРАВЛЕНО** в `64c39bf` + `fe341e1`.
- **[P3] Формулировка «полный прогон §16» шире фактического покрытия → ИСПРАВЛЕНО**: в `status.md` claim ослаблен до реально проверенного.
