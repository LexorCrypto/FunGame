# Codex audit — ae6b18a

- **Commit:** `ae6b18a` — fix(process): verify-after-write побайтовый — cmp сырого тела, subshell, свой mktemp (codex-аудиты 34fdd2f/7ed0267)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** REQUEST CHANGES — 1×P2, 1×P3.

## Findings (verbatim)

### Находки

- **[P2] [CLAUDE.md:105](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:105)** — запись и проверка могут обращаться к разным репозиториям. `gh issue edit 12` на строке 100 определяет репозиторий из cwd/`GH_REPO`, тогда как `gh api repos/LexorCrypto/FunGame/issues/12` жёстко читает канонический репозиторий. В форке или при заданном `GH_REPO` процедура способна изменить чужой issue #12, а затем успешно проверить другой. Особенно опасен идемпотентный случай, когда каноническое тело уже совпадает с кандидатом: `cmp` вернёт успех. Обеим командам нужен единый явно зафиксированный target, например `--repo LexorCrypto/FunGame`.

- **[P3] [CLAUDE.md:94](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:94)** — если первый `mktemp` успешен, а второй на строке 95 завершается ошибкой, `exit 1` происходит до установки `trap` на строке 96. Первый tempfile останется на диске. Он ещё пуст и имеет закрытые права, поэтому утечки секрета нет, но обещание «без следов» нарушено. Лучше установить cleanup сразу после первого файла или использовать один приватный `mktemp -d`.

**Вердикт: REQUEST CHANGES — 0×P1, 1×P2, 1×P3.**

Кратко:

- `docs/SPEC.md` не регулирует STATE/close-session; игровым требованиям коммит не противоречит.
- Утечек слушателей нет; найдена редкая утечка tempfile.
- Секретов и токенов в diff не обнаружено.

Измененные файлы:

- Коммит меняет только `CLAUDE.md`.
- Аудит файлов не изменял.

Проверки:

- `git show ae6b18a`, `git show --check`.
- Bash/zsh syntax check и ShellCheck — чисто.
- Gitleaks по `ae6b18a^..ae6b18a` — утечек нет.
- Сверен реальный CLI-контракт `validate_state_mfa.py`.
- Живой `gh issue edit` не выполнялся, поскольку это внешняя мутация.

Git/GitHub:

- Ветка `main`, коммит является текущим HEAD; ветка на 16 коммитов впереди `origin/main`.
- В дереве есть 17 ранее существовавших untracked-файлов; они не затрагивались.
- GitHub не изменялся.

## Disposition

- **[P2] Запись и проверка могли обращаться к разным репозиториям (`gh issue edit` берёт репо из cwd/`GH_REPO`, чтение было прибито к `LexorCrypto/FunGame`) → ИСПРАВЛЕНО** в `b4a959e`: репозиторий вынесен в переменную `R` и подставляется в обе команды.
- **[P3] Падение второго `mktemp` до установки `trap` оставляло первый файл на диске → ИСПРАВЛЕНО** в `b4a959e`: один приватный каталог `mktemp -d`, `trap 'rm -rf'` ставится сразу после создания. Проверено: каталог `drwx------`, удаляется на выходе.
