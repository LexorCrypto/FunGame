# Codex audit — 336b011

- **Commit:** `336b011` — docs: §17 честен про границу фаз у Трона, период топота и y круга Макаки (codex-аудит a2b94f9)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** REQUEST_CHANGES — 1×P2, 2×P3.

## Findings (verbatim)

### Находки

- [P2] `docs/SPEC.md:941` — SPEC допускает второй плюх Трона в том же окне после смены фазы, но `docs/SPEC.md:948` одновременно требует «один раз за окно». Нормативный документ не задаёт однозначный критерий приёмки. Ссылка на issue #10 не разрешает противоречие. Нужно либо сохранить флаг при смене фазы, либо явно записать исключение в таблице.

- [P3] `docs/SPEC.md:928` — `y=230` объявлен действующим контрактом, однако связанный GitHub issue #2 по-прежнему сформулирован как нерешённый выбор A/B. SPEC и живой трекер расходятся по статусу решения; issue следует закрыть или переоформить как запрос на изменение принятого контракта.

- [P3] `status.md:58` — жёстко записанное число открытых issues противоречит соседнему правилу «не дублировать, чтобы не расходиться с трекером» и неизбежно устаревает. В текущем незакоммиченном дереве это уже исправлено удалением счётчика.

Вердикт: **REQUEST_CHANGES** из-за [P2]. [P1] не найдено.

Кратко:

- Runtime-код коммит не меняет, поэтому новых утечек ресурсов, таймеров или слушателей нет.
- Секретов не найдено: `gitleaks` проверил один коммит, результат clean.
- `git diff-tree --check` — PASS.
- Serena активирована, но JavaScript language server недоступен; использован точечный `git show`/`rg`.

Изменённые файлы:

- Ничего не изменялось — аудит read-only.

Git/GitHub:

- Ветка `main`, `HEAD=336b011`, на 5 коммитов впереди `origin/main`.
- Рабочее дерево уже было и остаётся грязным; чужие изменения не затрагивались.

## Disposition

- **[P2] SPEC противоречил сам себе: проза допускала второй плюх на границе фаз, таблица требовала «один раз за окно» → ИСПРАВЛЕНО** в `df3d924`: требование осталось требованием, а фактическое поведение помечено как известное отклонение реализации со ссылкой на issue #10.
- **[P3] SPEC называл `y=230` действующим контрактом, а issue #2 был выбором «с нуля» → ИСПРАВЛЕНО** в `df3d924`: issue #2 переписан как заявка на пересмотр действующего значения.
- **[P3] Жёсткий счётчик открытых issues в `status.md` → ИСПРАВЛЕНО** в `df3d924` (убран; та же правка снята с хэндоффа и отчёта).
