# Codex audit — 965b18e

- **Commit:** `965b18e` — docs: project complete — FUN-26 prod acceptance passed, all 26 tickets Done (M1-M9)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** FAIL — коммит 965b18e нельзя считать корректным финальным checkpoint.** Он не меняет runtime-код, но ошибочно фиксирует полное соответствие SPEC и завершённую приёмку при наличии блокирующего пропуска Pause, непроверенных критериев и незакрытых runtime-дефектов.

## Findings (verbatim)

## Находки

- [P1] `.planning/.continue-here.md:16` — проект объявлен завершённым, хотя строка 19 признаёт отсутствие Pause. `docs/SPEC.md:789` требует `Esc → оверлей «ПАУЗА» → заморозка мира`; поиск по коду на ревизии нашёл только Esc для пропуска Crawl. Отсутствие тикета не отменяет SPEC. Вердикт: блокирует статус «соответствует SPEC».

- [P1] `status.md:48` — «разработка по SPEC закрыта», хотя `status.md:92` и `:97` прямо говорят, что обязательные §16.12 и §16.14 не проверены человеком. Headless-проверка не подтверждает соответствие звуков событиям, отсутствие щелчков и визуальную корректность эффектов. Вердикт: FUN-26 и полную приёмку закрыли преждевременно.

- [P2] `src/entities/BossBase.js:56` — каждый босс регистрирует анонимный `SHUTDOWN`-слушатель, но при `die()` он не снимается. Побеждённый босс также остаётся в `scene.bosses`: `WaveDirector.js:118-119` добавляет его, а смерть только делает объект неактивным. В бесконечном режиме сцена может не завершаться, поэтому боссы, closures и group entries накапливаются без границы. Вердикт: реальная утечка ресурсов в основном endless-flow; классификация как «минорная задержка сборки» занижена.

- [P2] `src/systems/WaveDirector.js:69` — модификаторы цикла применяются только к волнам 21–24; Золотой Трон получает неизменённый `GOLDEN_THRONE_WAVE`. Его `SWAY_PERIOD` и скорости остаются фиксированными, хотя `docs/SPEC.md:139-142` задаёт модификаторы для всего бесконечного цикла. Применяется только множитель очков ×c. Вердикт: частичное несоответствие §6.

- [P3] `status.md:59` — Known issues не очищены после завершения этапов: Трон всё ещё описан как не имеющий фабрики, а `status.md:75` утверждает, что аудио/мьют не реализованы. Это противоречит строкам 31–43 того же документа. Вердикт: финальный status/handoff внутренне несогласован.

## Вердикт

**FAIL — коммит 965b18e нельзя считать корректным финальным checkpoint.** Он не меняет runtime-код, но ошибочно фиксирует полное соответствие SPEC и завершённую приёмку при наличии блокирующего пропуска Pause, непроверенных критериев и незакрытых runtime-дефектов.

Секретов в diff не найдено: присутствуют только имя `ELEVENLABS_API_KEY`, публичный URL и утверждение о ранее раскрытом ключе — самого значения нет.

Кратко:

- Проверены `git show 965b18e`, SPEC и релевантные runtime-участки.
- Изменений не вносилось.

Проверки:

- Secret-pattern scan diff/tree — явных ключей нет.
- Runtime-тесты не запускались: коммит меняет только два Markdown-файла.

Git/GitHub:

- Ветка `main`, рабочее дерево чистое.
- Текущий HEAD опережает `origin/main` на 2 коммита.
- GitHub/Linear не изменялись.

## Disposition

- **[P1] Пауза §14 не реализована, но проект объявлен соответствующим SPEC → ИСПРАВЛЕНО** в `a669d93`.
- **[P1] «Разработка по SPEC закрыта» при непроверенных §16 п.12/п.14 → ИСПРАВЛЕНО (документация)**: формулировка снята, субъективные критерии вынесены в явный блок «ожидает владельца».
- **[P2] SHUTDOWN-слушатель босса + накопление боссов → ИСПРАВЛЕНО** в `64c39bf`.
- **[P2] Модификаторы цикла не действуют на Трон → ИСПРАВЛЕНО** в `905bb40`.
- **[P3] Known issues внутренне противоречивы (Трон «без фабрики», «аудио не реализовано») → ИСПРАВЛЕНО**: раздел пересобран в этой сессии.
