# Codex audit — 16e779c

- **Commit:** `16e779c` — fix(process): гейт STATE — fail-closed, закрытие issue — после прода и по команде (codex-аудит b50f3f3)
- **Auditor:** codex `gpt-5.6-sol`, reasoning=high, `-s read-only`
- **Date:** 2026-07-25
- **Verdict (codex):** REQUEST CHANGES — 1×P1, 1×P3.

## Findings (verbatim)

## Находки

- **[P1] [CLAUDE.md:85](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:85)** — «предварительный» гейт проверяет текущее тело issue #12, а не подготовленное новое тело. Инструкция предлагает опубликовать изменение и лишь затем перечитать и проверить его. Если кандидат содержит секрет или произвольный текст, он уже окажется в публичном issue; последующая ошибка валидатора утечку не предотвращает. Валидатор прямо предназначен для проверки полного outbound-кандидата до PATCH. Нужно сохранить кандидат локально, проверить его и передать тот же файл в `gh issue edit --body-file`, после чего повторить проверку удалённого тела. **Вердикт: блокирует принятие.**

- **[P3] [CLAUDE.md:84](/Users/aleksejsupilin/Documents/GitHub/Pisuarius/CLAUDE.md:84)** — утверждение «в теле не бывает значений — только ссылки, SHA и id» не соответствует схеме валидатора. Обязателен `last_updated` с датой; также разрешены LightRAG-путь/track, типизированный Supabase UUID и verification-токены. Корректная формулировка: «только типизированные указатели из схемы, без произвольного текста». **Вердикт: исправить документацию.**

Итоговый вердикт: **REQUEST CHANGES** из-за незакрытого сценария публикации секрета до валидации.

Кратко:

- Изменение порядка push → прод-проверка §16 → закрытие issue соответствует `docs/SPEC.md`.
- Секретов в самом diff не найдено.
- Runtime-код не затронут, поэтому новых утечек ресурсов или слушателей нет.

Изменённые файлы:

- `.planning/.continue-here.md`
- `CLAUDE.md`

Проверки:

- Прочитан полный `git show 16e779c`.
- Выполнен `git diff 16e779c^ 16e779c --check`.
- Сопоставлены изменения с `docs/SPEC.md:886–905`.
- Проверены фактическая CLI-схема и контракт `validate_state_mfa.py`.
- Выполнен поиск распространённых форматов секретов в diff.
- Runtime-тесты не запускались: коммит документальный.

Git/GitHub:

- Ветка `main`, `HEAD=16e779c`, локально на 10 коммитов впереди `origin/main`.
- В рабочем дереве есть существующие untracked-файлы; аудит их не изменял.
- GitHub-мутации не выполнялись.

Риски и следующие шаги:

- Добавить обязательную pre-PATCH-проверку точного нового payload.
- Исправить описание допустимых типизированных значений.
- После этого повторный аудит может дать `PASS`.

## Disposition

- **[P1] Гейт валидировал уже опубликованное тело, а не кандидата → ИСПРАВЛЕНО** в `808d6e4` (и доведено в `afe5422`): порядок стал «собрать кандидата в файл → валидатор по файлу → только при exit 0 писать → перечитать и проверить снова». Именно так тело #12 и правилось фактически.
- **[P3] «В теле не бывает значений — только ссылки, SHA и id» расходилось со схемой → ИСПРАВЛЕНО** в `808d6e4`: формулировка «нет произвольного текста, каждое значение — в своём типизированном формате» с перечислением форматов (дата, SHA, `repo#N`, LightRAG-путь + `track:`, `knowledge.<table>:<uuid>`, `ci:`/`tests:`/`codex:`).
- **Побочно найдено при проверке собственной документации:** команда из `808d6e4` не работала — путь надо передавать через `--file`, голый позиционный аргумент означает только stdin. Исправлено в `afe5422`, обе формы прогнаны на реальных данных.
